# 共通の指示

## 差分レビュー: `difit` を使う

ユーザーが「diff を見る」「変更を確認する」系の依頼をしたら `difit` を使う。GitHub 風の files-changed view をローカル Web サーバーで立てる CLI。

- バックグラウンド実行する(プロセスが立ちっぱなしになるため)
- 表示された URL をユーザーに伝える
- 引数の詳細は `difit --help`
- スクリプト的な比較・grep・テストでは引き続き `git diff` を使う
